<%= form_for @medicover_visits_watchdog, :html => { :class => "form-horizontal medicover_visits_watchdog" } do |f| %>

    <% if @medicover_visits_watchdog.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@medicover_visits_watchdog.errors.count, "error") %> prohibited this medicover_visits_watchdog from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @medicover_visits_watchdog.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :medicover_visit_query_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.collection_select :medicover_visit_query_id, MedicoverVisitQuery.all, :id, :specialization, {prompt: "Select a VisitQuery"}, {class: 'form-control'}%>
    </div>
    <%= error_span(@medicover_visits_watchdog[:medicover_visit_query_id]) %>
  </div>
  <div class="control-group">
    <%= f.label :first_possible_appointment_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :first_possible_appointment_date, :class => 'form-control' %>
    </div>
    <%= error_span(@medicover_visits_watchdog[:first_possible_appointment_date]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            medicover_visits_watchdogs_path, :class => 'btn btn-default' %>

<% end %>
